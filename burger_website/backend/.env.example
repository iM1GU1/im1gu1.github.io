PORT=3000
RESTAURANTS_CONFIG_PATH=./config/restaurants.json
DEFAULT_RESTAURANT_SLUG=burger-house
# Either provide a path to the JSON key file...
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
# ...or provide the JSON content directly (single line).
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
CACHE_TTL_SECONDS=45
NEXT_AVAILABLE_LOOKAHEAD_DAYS=30
# Optional: serve the frontend from Express
STATIC_DIR=..
